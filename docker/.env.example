# 数据目录与数据库
DATA_DIR=/data
DB_PATH=/data/app.db

# 初始管理员（首次启动会自动创建/确保存在）
# 请务必修改默认密码！
ADMIN_INIT_USER=admin
ADMIN_INIT_PASS=tgadmin

# 注意：已切换到 WTelegramClient 轻量级架构
# - 无需 TDLIB_DB_KEY（不再使用 TDLib）
# - 会话数据存储在 /data/accounts/{accountId}/session.dat
# - 支持 1C1G 服务器运行 50+ 账号
